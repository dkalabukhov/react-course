import { Layout } from '../layout/Layout';
import { RestaurantTabs } from '../restaurant-tabs/RestaurantTabs';

export const App = () => {
  return (
    <Layout>
      <RestaurantTabs />
    </Layout>
  );
};
